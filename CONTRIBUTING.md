---
title: "贡献指南（CONTRIBUTING）"
type: 治理
scope: 通用
source: "柴火团队整理"
date: 2026-09-14
tags: [贡献指南, 文档规范, 脱敏红线, 提交流程, 复核周期, CC BY 4.0]
---

# 贡献指南（CONTRIBUTING）

感谢你愿意为「创客空间搭建、管理与运营知识库」添砖加瓦。这个库的定位很明确：**结构化、可检索、可直接喂给 AI 的中文创客空间知识资产**。所以它的贡献门槛不在"文笔好不好"，而在三件事上——**有没有依据、有没有登记、有没有脱敏**。下面按提交前的顺序说清楚。

---

## 一、动手前的三件事

| 顺序 | 做什么 | 为什么 |
|------|--------|--------|
| 1 | 用 [GLOSSARY 检索索引](06-资源索引/GLOSSARY检索索引.md) 查一遍你的主题是否已有文档 | 本库是"术语 → 文档"一对一映射，**同一主题重复建文会破坏检索模型**；多数情况下应当是补充既有文档而非新建 |
| 2 | 跑一次全库体检：`python3 _kbcheck.py` | 先确认基线是绿的（0 ERROR / 0 WARN），这样你提交后一旦报错就能确定是自己引入的 |
| 3 | 在 [知识库覆盖度分析与补充路线图](00-知识库治理/知识库覆盖度分析与补充路线图.md) §三 的缺口清单里认领一项 | 缺口清单是本库的待办列表，认领后在 PR 里写明编号，维护者可以立刻判断价值 |

> 如果你只是想指出一个错误或过期信息，**不必走 PR**，直接开 Issue 更快，见 §六。

---

## 二、文档规范（硬性）

### 2.1 六字段 frontmatter

每篇 `.md` 必须以 YAML frontmatter 开头，六个字段一个都不能少（体检脚本第 6 类检查为 **ERROR** 级，缺字段直接拒绝提交）：

| 字段 | 含义 | 示例 |
|------|------|------|
| `title` | 文档标题，与 H1 一致 | `"设备摆放与布局规范"` |
| `type` | 文档类型，**受控词表 9 类**：入口 / 治理 / 方法论 / 规范 / 方案 / 案例 / 资源 / 索引 / 专题补充 | `规范` |
| `scope` | 适用范围：通用 / 本项目专项 | `通用` |
| `source` | 来源或依据，说明信息从哪来 | `"柴火团队整理 + ISO 10218"` |
| `date` | 建库或最后更新日期，**写到日**（只写年月会被判 INFO，无法做时效排序） | `2026-09-14` |
| `tags` | 检索标签数组，与 GLOSSARY 的术语对齐 | `[安全, 动线, 承重]` |

> **`type` 是受控词表，不是自由文本。** `_kbcheck.py` 第 6 类检查会在取值越界时直接报 ERROR 拒绝提交。九类的分工：`入口`（导航根）/ `治理`（元规则、事故记录）/ `方法论`（可复用的做法与流程）/ `规范`（须遵守的约束与模板）/ `方案`（针对特定场景的成套设计）/ `案例`（对标样本）/ `资源`（外部工具、文献、开源项目）/ `索引`（链接与术语汇总）/ `专题补充`（对既有主题的增量深化）。
>
> 历史教训：本库曾因脚本只校验「字段存在」而不校验「取值」，长出 13 类互不相同的取值（`专项方案`/`资源索引`/`调研`/`方法论文档`/`模板`/`教育专题`），规范形同虚设。2026-09-20 归并为上述 9 类并把校验写进脚本。

> **`scope` 怎么判？只有一条：看「锚点在谁身上」。**（2026-09-22 立）
>
> | 锚点在哪 | 取值 | 例 |
> |---|---|---|
> | **公开标准 / 公开素材 / 通用规律** —— 换个人、换个项目也照样成立 | `通用` | 教育部专业目录与 1+X 证书、现行国标条文（GB 55037 等）、外部开源项目核查、通行工艺与安全做法 |
> | **某一委托项目的具体条件** —— 引用该项目自己的设备清单条目 / 分区命名 / 面积账 / 预算 / 甲方名称与场地约束 | `本项目专项` | 「某项目 A 区 A-04 台架的退让带」、某项目的落位展开 |
>
> **一句话自查**：把这篇文档交给**另一个创客空间**，它是「照用」还是「照废」？照用 → `通用`；照废（因为引用的是某个项目的具体配置）→ `本项目专项`。
>
> ★ **`通用` 不等于「内容浅」**：一篇 `通用` 文档可以很深（例如完整的设备危害四级与防护要求），但它的**每一处依据都能脱离本项目独立成立**。反过来，把某个项目的成套展开（如 189 格技能树、逐设备的项目授权对照）写成 `通用`，就是本条的典型违规 —— 那是**项目交付物**，应当放在项目目录，**不属本库资产**。
>
> ★ **移出登记的去向纪律**：文档移出本库后，本库里只留**一行纯文本登记**（**不写 markdown 链接** —— 否则第 2 类检查会判「库外引用」）。登记里**只写去向类型，不写去向路径** —— 例如「已移至**委托项目的私有项目仓**」，而不写出那个私有仓的目录名。原因是本库是公开仓，写出路径等于公开私有项目结构（**这条由第 8 类脱敏检查强制兜底**，本轮实测被拦下 3 处）。
> ★ 同批纪律：移出后**不得改写历史日志行**（历史就是历史），移出动作本身另记一条新日志。

> **为什么这条要写死**：判错方向的代价不对称 —— 把 `通用` 错标成 `本项目专项`，代价只是检索变差；把 `本项目专项` 错标成 `通用`，会让读者以为可以照抄，且在公开仓里混入委托方信息。



---

### 2.2 命名与篇幅

- **文件名不带受众后缀、不带版本号**：写 `机器人实训区设计.md`，不写 `机器人实训区设计-给甲方看-v3.md`。受众与版本信息写在 frontmatter 里（见 [口径事故与返工记录](00-知识库治理/口径事故与返工记录.md) A-06：复合后缀会切断脚本的自动归族）。
- **单篇超过 30,000 字建议拆分**为「总篇 + 分册」：总篇保留定位、结构与落地建议并充当系列入口，分册按主题切分，各分册加系列导航条互相链接。体检脚本会在统计区列出超长文档。
- **表格优先**：能用表格表达的对照关系不要用长段落。注意 markdown 表格的硬规则——**表头列数 = 分隔行列数 = 每个数据行列数**（第 9 类检查为 ERROR 级）；单元格里如果必须出现竖线，请写成转义形式 `\|`；代码围栏内的内容不会被误判。

### 2.3 可溯源与可复现

- **每个数字、每条结论都要能追到出处**：标准编号、官方文档、实测记录、公开报道皆可，无法溯源的内容请标注为"经验估计"并说明依据。
- **能被命令复现的数字，把命令一起写上**：例如"全库 97 篇 / 659,069 字"必须同时给出 `python3 _kbcheck.py --stats-md`，否则半年后没人能验证它是否过期。
- 引用外部资料时注明版本/年份；标准与政策类内容注明发布机构与施行日期。

### 2.4 登记三件事（漏一件就检索不到）

新增或重命名文档后，必须同步三处，否则体检脚本会报 WARN 并拒绝提交：

1. 在 [README.md](README.md) §二「知识库结构导航」树里补一行（**一行只放一个 `.md` 条目**，两个挤在一行会触发树排版 WARN）；
2. 在 [GLOSSARY 检索索引](06-资源索引/GLOSSARY检索索引.md) 对应主题分组补一条"术语 → 文档"映射；
3. 在 [知识库覆盖度分析与补充路线图](00-知识库治理/知识库覆盖度分析与补充路线图.md) §6.1 更新日志加一行（倒序，最新在上）。

此外，新文档必须**至少有一个入链**（从别的文档链过来），否则会被判为"孤岛文档"。

---

## 三、脱敏红线（最容易踩，且不可逆）

本库是**公开仓库**（CC BY 4.0）。以下内容一旦提交，即便事后删除当前版，**仍会永久留在 git 历史与托管平台的对象缓存里**，只能靠重写历史 + 删仓重建修复——代价极高。相关事故见 [口径事故与返工记录](00-知识库治理/口径事故与返工记录.md) A-11 / A-12 / A-14，处置流程见 [公开前安全核查与历史遗留对象处置规程](00-知识库治理/公开前安全核查与历史遗留对象处置规程.md)。

| 禁止出现 | 改写成 |
|----------|--------|
| 真实委托方 / 甲方 / 合作机构的项目专名 | 中性称谓："某职业院校""某科普场馆""某企业客户" |
| 本地绝对路径（`/Users/xxx/...`、`D:\...`） | 仓库内相对路径 |
| 个人隐私：私人手机号、身份证、住址、私人邮箱、家庭成员信息 | 删除，或改用 GitHub Issues 等公开渠道 |
| 旧提交哈希 / 内部对象 SHA | 用「日期 + 变更内容」定位，不写哈希 |
| 商务报价、合同金额、投标底价等非公开商业信息 | 只保留可公开的区间或方法论，注明来源性质 |
| 他人署名内容未经许可的整段转载 | 摘录要点并注明出处；整段转载需原作者授权 |

**自检方式**：维护者侧有一份不随仓库分发的词表 `_local_secrets.txt`，体检脚本第 8 类检查会用它扫描"会被发布的文件集"（已跟踪 ∪ 未跟踪未忽略，覆盖全部文本载体），命中即 ERROR。**外部贡献者没有这份词表，脱敏检查会自动跳过——所以请对照上表逐条自查**，不要依赖脚本。

---

## 四、提交与 Pull Request 流程

```bash
# 1. Fork 本仓库，克隆自己的 fork
git clone https://github.com/<你的账号>/makerspace-knowledge-base.git
cd makerspace-knowledge-base

# 2. 开一个短命名分支（一 PR 一主题，不要混装多个改动）
git checkout -b add/robot-safety-zone

# 3. 写完后自检，必须 0 ERROR / 0 WARN
python3 _kbcheck.py
python3 _kbcheck.py --stats-md    # 需要更新规模数字时用

# 4. 提交并推送，然后在 GitHub 上开 PR
git add -A && git commit -m "新增 03-设备与工具/机器人安全围栏规范：……"
git push origin add/robot-safety-zone
```

**提交信息请写清"改了什么 + 为什么 + 影响面"**，中文即可，方便后来者在 `git log` 里定位。

> ⚠️ **关于 pre-commit hook**：仓库内的 `_hooks/pre-commit` 是**维护者侧**的版本化副本，除体检外还强制校验提交身份白名单（见 A-14）。**外部贡献者不需要安装它**；如果出于习惯装了并被身份校验拦下，请用 `KB_ALLOW_IDENTITY=1 git commit ...`，或直接 `rm .git/hooks/pre-commit` 卸载。你只需保证 `python3 _kbcheck.py` 退出码为 0。

**PR 描述里建议包含**：① 解决的缺口编号（若有）；② 新增/修改的文件清单；③ 自检结果（`0 ERROR / 0 WARN`）；④ 三处登记是否已完成。

---

## 五、复核周期（改旧文档时请看这里）

本库按 §6.3 约定做定期核验：

| 内容类型 | 复核周期 | 说明 |
|----------|----------|------|
| 时效敏感：政策、标准、赛事、价格、Fab Academy 年度信息 | **每 6 个月** | 更新后登记到 §6.1 |
| 方法论 / 理念类 | **每 24 个月** | 无强时效，两年提示一次 |

维护者可跑 `python3 _kbcheck.py --review-due` 列出到期文档（时效敏感清单直接从治理文档解析，清单改了脚本自动跟随；文档年龄取自 git 最后提交日）。发现信息过期时，**请优先修订并更新日期与 §6.1**，而不是另开一篇。

---

## 六、Issue 用法

以下情形开 Issue 比开 PR 更合适：

- **勘误**：数字错了、标准号写错了、链接失效了（请附文档路径与行号）；
- **口径冲突**：发现两处文档对同一事实给出矛盾表述（这是本库最重视的一类，见 A-01 / A-02）；
- **方向讨论**：认为某个维度缺整块内容、或某个分类应该重组；
- **授权与版权**：认为某处引用超出了合理范围，或你希望撤回自己贡献的内容；
- **使用反馈**：你用本库配合 AI 完成了实际方案，欢迎分享工作流与提示词，我们会收录进 [AI辅助创客空间规划-方法与工具库](02-空间搭建/AI辅助创客空间规划-方法与工具库.md)。

---

## 七、许可与署名

**向本仓库提交内容，即表示你同意自己的贡献以 CC BY 4.0 协议发布**，并同意维护者在保留署名的前提下对其进行结构化整理（补 frontmatter、拆分超长文、修正表格、脱敏改写）。

- 完整协议文本见 [LICENSE](LICENSE)；版权、署名要求与第三方资料说明见 [NOTICE](NOTICE)。
- 请在 PR 描述里注明你希望被如何署名（GitHub 账号、真名或"匿名贡献者"均可）。
- 引用本库的规范格式见 [README.md](README.md) §六 与 `CITATION.cff`。

> ⚠️ **第三方许可红线：`NC` 与 `SA` 会反向拉低本库的授权（2026-09-22 增补）**
>
> 本库采用 CC BY 4.0——**含商业用途**，且**不要求**衍生作品采用相同许可。因此，把含 **`NC`（非商业性使用）** 或 **`SA`（相同方式共享）** 条款的第三方材料**复制进本库**，会**反向拉低本库自身的授权**：`NC` 使"可商用"失效，`SA` 强迫本库改用同一许可——两者都会传染到包含它们的整个作品。
>
> **处置**：这类材料**一律不把素材复制进本库**（`CC BY-NC-SA` 全不可纳入；`CC BY-SA` 亦因 `SA` 传染而不可纳入）。但**提炼其原理、要点与结构，注明出处并给出外链指向，不受此限**——受版权保护的是**表达**，不是**方法**。
>
> **判据一句话**：看许可名里有没有 `NC` 或 `SA` 两个字母，出现任意一个，就只做"原理借鉴 + 外链指向"。
>
> **两条易漏点**：① **翻译版与原文同受约束**——有中文版不等于可自由使用；② 作者声明"允许你把打印件用在教学 / 创客空间场景"**不等于**"允许你把素材放进另一个开源库"——**这是两个不同层级的授权，不能互相推导**。
>
> 案例与完整论证见 [创客技能树-技能可视化与进度追踪-专题补充.md](04-运营与管理/教育专题/创客技能树-技能可视化与进度追踪-专题补充.md) §二。

---

## 八、不接受的内容

为了保持本库"可直接作为 AI 上下文"的质量，以下内容会被婉拒：

- 无可溯源依据的个人断言，且无法标注为"经验估计"的；
- 营销软文、产品推广、导流链接；
- 与创客空间"规划 / 建设 / 管理"三个环节无关的内容；
- 含 §三 任一红线信息的；
- 大段整篇转载第三方受版权保护材料的（摘录要点 + 注明出处可以）；
- 未经脱敏的真实项目档案（项目专项内容请走项目私有仓，见 A-11 ⑤「公开仓与项目仓物理分离」）。

---

> **一句话总结**：先查重、再动笔、写完跑体检、三处登记、对照红线自查脱敏——五步做完，PR 基本一次过。
